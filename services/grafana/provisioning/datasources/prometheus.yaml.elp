apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: <%= (for-service :prometheus internal_url) %>
    isDefault: true
    editable: true
