apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # /prometheus prefix matches prometheus's --web.route-prefix flag;
    # without it grafana hits /api/v1/... and gets 404.
    url: http://prometheus:9090/prometheus
    isDefault: true
    editable: true
