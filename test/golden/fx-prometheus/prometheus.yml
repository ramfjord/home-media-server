global:
  scrape_interval: 30s
  external_labels:
    deployment: fixture-deployment
    host: fx-host

scrape_configs:
  - job_name: "fx-prometheus"
    metrics_path: /-/healthy
    static_configs:
      - targets: ["fx-prometheus:19090"]
        labels:
          partof: monitoring
