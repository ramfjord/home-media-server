<%- (loopservices (services :where (and use_vpn port)) -%>
:<%= port %> {
  reverse_proxy fx-wireguard:<%= port %>
  tls /etc/caddy/certs/fullchain.pem /etc/caddy/certs/privkey.pem
}
<%- ) -%>
# host: <%= hostname %>
# install_base: <%= install_base %>
