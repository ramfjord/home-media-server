---
services:
  fx-qbittorrent:
    container_name: fx-qbittorrent
    environment:
    - TZ=Etc/UTC
    user: '99003'
    network_mode: container:wireguard
    image: example.invalid/fx-qbittorrent:latest
    volumes:
    - /opt/fx-mediaserver/config/fx-qbittorrent:/config
    - /data:/data
    restart: unless-stopped
...
