tmp
config.local.yml
config/
elp

docker-compose.yml

# Real certs live alongside caddy's other config; gitignored.
# Test fixture certs (test/services/fx-caddy/certs/) are checked in.
services/caddy/certs/

# Fixture tree (test goldens) overrides the above ignores.
!test/config.local.yml
!test/config/
!test/config/**
.make.services
Makefile.local
audio-normalizer/examples
.swank-port
.mcp.json
.claude

# Lisp build artifacts
*.fasl
bin/
test/config-lisp/
.swank-session
