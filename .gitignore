tmp
config.local.yml
config/
**/services/manifest.yaml
**/services/manifest.yaml.elp
elp
.claude

docker-compose.yml

# Fixture tree (test goldens) overrides the above ignores.
!test/config.local.yml
!test/config/
!test/config/**
.make.services
Makefile.local
audio-normalizer/examples
.swank-port
.swank-lsp-port
.vlime-port
.mcp.json

# Lisp build artifacts
*.fasl
bin/
test/config-lisp/
.swank-session
.swank-lsp
.qlot/
.qlot-cache/

# Dev container HOME (shell history, vim state, .quicklisp cache, etc.)
.devhome/
.env
tools/api-config/.image-built

# Legacy cert location — pre tailscale-cert migration certs may still
# sit here on dev machines that haven't cleaned up. Real certs now
# live under <install_base>/certs/ on the deploy target. Hard-block
# to keep keys out of commits.
services/caddy/certs/
