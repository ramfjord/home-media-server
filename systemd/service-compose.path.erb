[Unit]
Description=Docker compose change watcher for <%= service.name %>

[Path]
PathChanged=<%= install_base %>/config/docker-compose.yml
Unit=<%= service.name %>.service

[Install]
WantedBy=<%= service.name %>.service
RequiredBy=<%= service.name %>.service
