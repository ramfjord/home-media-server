[pytest]
# asyncio_mode = auto: plain `async def test_*` run without a per-test
# marker. The suite is in-process (httpx.MockTransport) — no network,
# no Docker; runs the same host-native or in CI.
asyncio_mode = auto
