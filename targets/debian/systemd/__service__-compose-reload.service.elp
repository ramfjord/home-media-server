<%- (when (and dockerized (not has_unit)) -%>
[Unit]
Description=Restart docker compose service <%= name %>
After=<%= name %>.service

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl restart <%= name %>.service
<% ) -%>
