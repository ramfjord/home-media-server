<%- (when (and dockerized (not has_unit)) -%>
[Unit]
Description=Docker compose change watcher for <%= name %>

[Path]
PathChanged=<%= compose_file %>
Unit=<%= name %>-compose-reload.service

[Install]
WantedBy=<%= name %>.service
RequiredBy=<%= name %>.service
<% ) -%>
